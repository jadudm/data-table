#lang info

(define collection "data-table")

(define deps '("csv-reading"
               "gregor"
               ))

(define scribblings '(("scribblings/data-table.scrbl" 
                      (multi-page)
                      )))